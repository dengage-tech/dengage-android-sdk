<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>

    <activity android:name=".manager.GeofencePermissionActivity"
        android:theme="@style/Theme.Transparent"
        android:noHistory="true"/>
    <receiver
        android:name=".manager.GeofenceLocationReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter android:priority="999">
            <action android:name="com.dengage.geofence.manager.GeofenceLocationReceiver.SYNCED_GEOFENCES" />
            <action android:name="com.dengage.geofence.manager.GeofenceLocationReceiver.GEOFENCE" />
            <action android:name="com.dengage.geofence.manager.GeofenceLocationReceiver.LOCATION" />
            <action android:name="android.intent.action.BOOT_COMPLETED" />
        </intent-filter>
    </receiver>


    <service android:name=".manager.GeofenceJobScheduler"
        android:permission="android.permission.BIND_JOB_SERVICE"
        android:exported="true"
        tools:targetApi="lollipop" />

    <!-- Geofence Engine v2 (doc 21 §6.6) -->
    <!-- PROVIDERS_CHANGED + MY_PACKAGE_REPLACED: konum servisleri toggle'ı ve uygulama
         güncellemesi OS'taki geofence kayıtlarını silebilir; full re-register tetiklenir
         (doc 23 İş 1). Her ikisi de implicit-broadcast yasağından muaftır. -->
    <receiver
        android:name="com.dengage.geofenceengine.receiver.GeofenceBroadcastReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED" />
            <action android:name="android.location.PROVIDERS_CHANGED" />
            <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        </intent-filter>
    </receiver>

    </application>

</manifest>